{
  "name": "top-autocare-functions",
  "scripts": {
    "build": "npm run lint",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "nodemailer": "^6.9.0",
    "puppeteer": "^22.0.0", // For PDF reports
    "twilio": "^4.20.0", // Optional: SMS (add credentials in .env)
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0",
    "eslint": "^8.15.0"
  },
  "private": true
}
